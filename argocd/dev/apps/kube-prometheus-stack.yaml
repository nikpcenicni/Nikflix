# Prometheus + Grafana + Alertmanager, values in ../values/kube-prometheus-stack-values.yaml.
# See docs/architecture/talos.md's "Core platform applications" (Prometheus, Grafana).
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: kube-prometheus-stack
  namespace: argocd
spec:
  project: default
  sources:
    - repoURL: https://prometheus-community.github.io/helm-charts
      chart: kube-prometheus-stack
      # TODO: pin to the version already installed by hand (`helm list -n
      # monitoring`) before letting ArgoCD sync this - an unpinned/wrong
      # version could trigger an unwanted chart upgrade against live
      # Prometheus/Grafana state.
      targetRevision: REPLACE_ME
      helm:
        valueFiles:
          - $values/argocd/dev/values/kube-prometheus-stack-values.yaml
    - repoURL: https://github.com/nikpcenicni/Nikflix.git
      targetRevision: main
      ref: values
  destination:
    server: https://kubernetes.default.svc
    namespace: monitoring
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
